# Copy to .env and fill in. .env itself is gitignored - never commit real
# secrets. Generate JWT_SECRET_KEY with:
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
