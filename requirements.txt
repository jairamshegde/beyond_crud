# ==============================================================================
# FastAPI Core Stack & Configurations
# ==============================================================================
fastapi==0.141.1       # Core framework + built-in standard server components
uvicorn[standard]==0.52.3      # ASGI server used to run the app (ships uvloop/httptools)
pydantic>=2.7.0,<3.0.0               # Fast Rust-backed core data validation
pydantic-settings>=2.2.1      # Advanced environment variable and secrets management
email-validator==2.3.0          # Backs Pydantic's EmailStr - real email format validation

# ==============================================================================
# Database & Async PostgreSQL Migrations
# ==============================================================================
sqlalchemy==2.0.32              # SQL Toolkit and Object-Relational Mapper (ORM)
alembic==1.14.0                 # Database migrations tracking
asyncpg==0.30.0                 # High-performance, async engine for PostgreSQL

# ==============================================================================
# Security & Hashing (Argon2 Integration)
# ==============================================================================
passlib==1.7.4                  # Password hashing engine abstraction layer
argon2-cffi==25.1.0             # Highly secure Argon2id bindings utilized by passlib
pyjwt==2.10.1                   # JSON Web Token (JWT) handling
python-multipart==0.0.18       # Support for multi-part forms and file uploads

# ==============================================================================
# Distributed Background Tasks
# ==============================================================================
celery[redis]==5.6.3                   # Asynchronous distributed task worker engine
redis==5.2.1                    # Highly efficient broker / caching layer for Celery

# ==============================================================================
# Logging (Phase 6)
# ==============================================================================
loguru==0.7.3                   # Structured logging with non-blocking (enqueue) sinks

# ==============================================================================
# Testing (Phase 4)
# ==============================================================================
pytest==8.3.4                   # Test runner
httpx2                          # HTTP client FastAPI's TestClient is built on - httpx's
                                 # maintained successor (plain httpx is deprecated for this
                                 # use as of Starlette's May 2026 TestClient support)

